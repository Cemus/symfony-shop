{% extends 'base.html.twig' %} {% block title %}Ajouter un article - Super Site
{% endblock %} {% block body %}

<main class="flex flex-col flex-1 items-center justify-center">
  <turbo-frame id="id-article">
    <div>
      {{ form_start(form) }}
      {{ form_end(form) }}
    </div>
    {% include 'layout/_toast.html.twig' %}
  </turbo-frame>
</main>

{% endblock %}
